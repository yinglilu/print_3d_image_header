name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: build
      run: gcc -o printh printh.c nifti1_io.c znzlib.c -lm
    - name: run it
      run: ./printh
